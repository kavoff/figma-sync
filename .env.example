# GitHub PR Sync Configuration
# Optional - Direct PR Mode will only activate if these are set

# GitHub Personal Access Token with repo permissions
GITHUB_TOKEN=your_github_token_here

# Target repository in format "owner/repo"
GITHUB_REPO=owner/repo

# Base branch to create PRs from (default: main)
GITHUB_BRANCH=main

# Target file path in the repository for JSON artifacts
GITHUB_TARGET_PATH=artifacts/texts.json

# Commit message template (supports {key} and {timestamp} placeholders)
GITHUB_COMMIT_MESSAGE_TEMPLATE="Update text artifact for {key} - {timestamp}"

# PR title template (supports {key} and {timestamp} placeholders)  
GITHUB_PR_TITLE_TEMPLATE="Text Update: {key}"

# PR body template (supports {key} and {timestamp} placeholders)
GITHUB_PR_BODY_TEMPLATE="Automated text update for {key} at {timestamp}"